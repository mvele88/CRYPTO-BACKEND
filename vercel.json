// api/index.js

const express = require('express');
const app = express();

// A simple root route that must work
app.get('/', (req, res) => {
  res.send('This is the root route. It works!');
});

// Export the app as a module
module.exports = app;